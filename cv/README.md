# cv

Use this directory for a cybersecurity-focused CV and related materials.

## GitHub Pages

This folder is deployed as a GitHub Pages site via `.github/workflows/deploy-cv-pages.yml`.

- Source: `/cv`
- Trigger: pushes to `main` that change files under `/cv` (or manual workflow dispatch)
- Publish URL: `https://<owner>.github.io/<repo>/`
